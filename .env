# Path to espeak-ng shared library (DLL) for phonemizer on Windows
# Update the path if your `libespeak-ng.dll` is installed elsewhere.
PHONEMIZER_ESPEAK_LIBRARY="C:\\Program Files\\eSpeak NG\\libespeak-ng.dll"